See [AGENTS.md](./AGENTS.md) for how to use this library (which function to call, paired vs.
grouped design, return formats, minimal snippets). This file exists only so Claude Code's
project-memory auto-load also picks it up; the content lives in `AGENTS.md` so Codex and
other tools see the same thing.
